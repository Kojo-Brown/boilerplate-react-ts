/**
 * The product name, in one place.
 *
 * It is the brand in the navbar and the suffix on every document title, and
 * those two drifting apart is the sort of thing nobody notices until a
 * screenshot goes in a deck. `index.html` still carries a literal `<title>`,
 * which is not duplication to remove: it is what the browser shows for the
 * fraction of a second before any JavaScript runs, and it cannot read this.
 */
export const APP_NAME = "React TS";
